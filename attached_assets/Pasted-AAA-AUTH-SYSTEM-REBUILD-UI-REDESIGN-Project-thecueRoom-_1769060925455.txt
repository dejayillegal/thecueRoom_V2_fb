AAA AUTH SYSTEM REBUILD + UI REDESIGN

Project: thecueRoom_V2
Mode: Free / Fast (90% used safe)
Scope: Full auth reimplementation + UI redesign
Quality Bar: Production AAA

🔒 SYSTEM / AUTHORITY LOCK (ABSOLUTE)

You are working inside thecueRoom_V2.

You MUST obey this precedence order at all times:

docs/CHATGPT_PROJECT_RULES.md

docs/thecue_room_master_codex_prompt_mobile_web_backend_zero_error_build.md

Existing repository implementation (audit first)

Supabase official documentation

Next.js App Router documentation

If instructions conflict, higher precedence wins immediately.

🧠 CONTEXT RESET (CRITICAL — READ FULLY)
Current confirmed issues

Duplicate close (×) icons in auth modal

Fake / misleading security text (“Secure Session”, “System Nominal”, versions)

UI implying security systems that do not exist

Overlapping labels, inputs, and buttons

Signup missing existing required fields

Excess vertical spacing → forced scroll

Placeholder text overpowering real input values

Motion overpowering readability

Auth modal visually drifting from landing/header identity

Fragmented, brittle auth logic

Feeds/auth failures on fresh setup due to schema mismatch

This task MUST fix all of the above.
🚫 PREVIOUS LIMITATIONS — OFFICIALLY OVERRIDDEN

You are explicitly authorized to:

✅ Redesign auth UI/UX
✅ Rewrite auth logic cleanly
✅ Modify API routes
✅ Modify database schema
✅ Modify Supabase auth configuration
✅ Implement progressive trust levels
✅ Implement risk-adaptive login
✅ Implement backend-verified artist onboarding

You are NOT allowed to:

❌ Add new npm dependencies
❌ Use paid Supabase features
❌ Add cron jobs, workers, queues
❌ Invent fake security language
❌ Imply AI or verification that is not enforced
❌ Break dashboard routing

🌍 PLATFORM GUARANTEE (NON-NEGOTIABLE)

The system MUST run without errors on:

Replit (Free)

Local Node

Vercel Hobby

Railway Free

Supabase Free

Assume:

No background jobs

No scheduled tasks

No edge-only features

No enterprise flags

Everything must be request-driven and deterministic.

🧩 PHASE 1 — FULL AUTH & DATA AUDIT (MANDATORY)

Before writing code:

Audit frontend

Existing auth modal components

Form fields and validation

Error handling

Motion usage

Audit backend

Supabase auth setup

User tables

Artist/profile tables

Existing API routes

Broken feed schema references

Output

Inline comments only

Clearly mark: keep / remove / rewrite

No UI changes yet
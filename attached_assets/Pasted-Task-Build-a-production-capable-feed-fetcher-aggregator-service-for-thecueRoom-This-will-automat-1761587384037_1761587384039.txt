Task: Build a production-capable feed fetcher & aggregator service for thecueRoom. This will automatically fetch the supplied RSS sources (primary), fall back to scraping when RSS is unavailable, cache and revalidate efficiently, provide paginated normalized JSON APIs consumed by the landing page, and keep feeds updated via on-demand refresh + background schedule. Must be runnable in Replit and locally on macOS/Linux, and include tests and a simple deployment-ready GitHub Actions workflow.

Tech stack (required)
- Node 20 (TypeScript)
- Next.js (App Router) or Express + server-rendered landing page (choose Next.js if possible in Replit)
- API routes under `/api/*` (Next.js Edge functions optional) for feed endpoints
- Storage: SQLite (via better-sqlite3) for seed + persisted feed metadata + normalization. Provide optional Redis support (for Replit, use in-memory fallback).
- RSS parsing: `rss-parser` (pinned version).
- Scraping: `cheerio` + `got` or `node-fetch` + `playwright` headless fallback for JS-heavy pages (playwright used only in test-mode or optional runtime when environment variable ENABLE_PLAYWRIGHT=true).
- Rate limiting & queueing: `bottleneck` for per-domain throttling + in-process queue (with optional Redis-backed BullMQ if REDIS_URL present).
- HTTP client: `got` (pinned).
- Thumbnail extraction: prefer `og:image` meta, then `<img>` selectors, then placeholder.
- Caching & revalidation: Use conditional requests (If-Modified-Since, If-None-Match/ETag). Store `lastFetched`, `etag`, `lastModified`, `cacheTTL` per-source in DB. Implement `stale-while-revalidate`: serve stale cached items immediately and trigger background revalidate.
- Robots.txt: Respect robots (fetch and parse /robots.txt) before scraping; do not crawl disallowed paths.
- Politeness: per-domain backoff and exponential backoff on 429/5xx.
- Tests: Vitest for unit tests, Playwright for an E2E fetch flow (with mocked network).
- CI: GitHub Actions to run lint, typecheck, tests, and build.

Primary behavior & endpoints
1. `/api/feeds` — GET — returns paginated normalized feed items across all sources (query params: `page`, `limit`, `tags`, `since`, `q`).
2. `/api/feeds/:sourceId` — GET — returns items for a single source (with `If-Modified-Since` support).
3. `/api/sources` — GET — returns configured sources and metadata (tags, lastFetched, status).
4. `/api/refresh` — POST — admin-only (test-mode bypass) to trigger refresh for either `all` or a specific `sourceId`. Rate-limited.
5. `/api/health` — GET — health checks (db ok, queue ok).
6. Server-side landing page: when landing page is requested, fetch initial page of `/api/feeds` server-side to render SSR and improve perceived performance.

Required features (must be implemented)
- Seed the DB with the entire SPOTLIGHT_SOURCES list you provided (all RSS & scrape entries) at first-run.
- The fetcher must attempt `rss` first. If that fails (no feed, bad status, not parsable) then use `scrape` selectors for that source. If scrape fails and ENABLE_PLAYWRIGHT=true, spin Playwright (headless) to render JS pages and then scrape.
- Every fetch MUST:
  - Use conditional headers (If-None-Match / If-Modified-Since) if `etag` or `lastModified` exists.
  - Respect `cache-control`/`s-maxage` and have a default TTL (30 minutes) if none provided.
  - On 304, update `lastFetched` but keep items unchanged.
  - On success, normalize items to this shape:
    ```ts
    type FeedItem = {
      id: string; // deterministic id — e.g. sha256(canonicalUrl || title+date)
      title: string;
      excerpt?: string;
      content?: string;
      url: string;
      source: { id: string; name: string };
      image?: string;
      tags: string[];
      date: string; // ISO
      fetchedAt: string; // ISO
    }
    ```
- Deduplication: dedupe items by canonical URL, then by title similarity. Provide configurable similarity threshold in config.
- Thumbnail handling: populate `image` using `og:image` > `<meta itemprop="image">` > `<img>` > placeholder.
- Background scheduler:
  - Use node-cron (or setInterval) to run a refresh job every 5–30 minutes (configurable). Implement jitter + distributed lock (use SQLite row lock or Redis lock if REDIS_URL present) to prevent multiple instances revalidating same source.
  - Support per-source frequency override (some sources can be set to update every 10m, others every 6h).
- Stale-while-revalidate:
  - API serves cached items instantly.
  - When cache expired, trigger background refresh but also return cached content; client receives header `X-Stale-While-Revalidate: true` when stale served.
- Error handling & monitoring:
  - Track error counts per source and backoff if error rate high.
  - Emit logs and metrics (console logs + optional stats endpoint).
- Admin safety:
  - `/api/refresh` protected by a shared secret token provided via `REFRESH_API_KEY` env var; in Replit dev you may set `REFRESH_API_KEY=test-mode`.
- Respect robots.txt and provide an admin override (only if `ALLOW_ROBOT_OVERRIDE=true`).

Project structure (required)
/ (repo root)
package.json
tsconfig.json
.env.example
/src
/server
server.ts (Next.js custom server or Express)
/api
feeds.ts
feedById.ts
sources.ts
refresh.ts
health.ts
/lib
db.ts (better-sqlite3 wrapper)
models.ts (types & interfaces)
rssFetcher.ts (rss parsing + conditional requests)
scraper.ts (cheerio scraping logic)
playwrightScraper.ts (optional headless rendering)
queue.ts (bottleneck queue)
dedupe.ts (similarity + hashing)
thumbnail.ts (og:image extractor)
robots.ts (robots.txt fetch + parser)
scheduler.ts (cron)
seed.ts (seed sources)
/config
spotlight-sources.ts (the full list you provided)
/tests
unit (zod and fetcher unit tests)
e2e (playwright and API tests)
/.github/workflows/ci.yml
README.md (setup & run, env vars, scaling notes)

typescript
Copy code

Seed data
- Use the SPOTLIGHT_SOURCES exactly as you provided; make `id` a slug (e.g., `6am-group`, `the-playground`) and store `kind`, `url`, `tags`, and for scrape sources the `list`, `title`, `link`, `image`, `summary`, `date` selectors.

Acceptance criteria (automated)
- `pnpm install` then `pnpm start` boots server, seeds DB, and serves `/api/feeds` with at least one item per seeded RSS source (or a clear status showing source unreachable).
- `pnpm test` runs all unit tests and E2E mocks (no external network dependency in CI; use nock or msw to mock external feeds).
- Lint & Typecheck pass (`pnpm lint`, `pnpm -w tsc --noEmit`).
- Background scheduler correctly updates `lastFetched` timestamps and stores ETag/lastModified.
- Deduplication prevents duplicates when a remote feed includes same item multiple times.

Dev & run commands (must be included in generated repo)
- `pnpm install`
- `cp .env.example .env` and edit (or rely on Replit secrets)
- Dev run (single command): `pnpm dev` — starts server and scheduler
- Seed DB manually: `pnpm run seed`
- Trigger refresh (local): `curl -X POST -H "x-refresh-key: <REFRESH_API_KEY>" http://localhost:3000/api/refresh?sourceId=6am-group`
- Run tests: `pnpm test`
- Run in production mode: `pnpm start` (NODE_ENV=production)

Implementation hints (enforce in the generated code)
- Use `rss-parser` with `requestOptions` that include `timeout` and `headers`.
- Use `got` for HTTP fetches; pass conditional headers when available.
- Use `cheerio` for scraping. For scrape fallback, attempt multiple selector sets as defined in seed.
- Implement per-domain Bottleneck limiter: `const limiter = new Bottleneck.Group({ maxConcurrent: 2, minTime: 500 })` and key by domain.
- Use `better-sqlite3` for synchronous simple DB access in Replit; provide adapter in `/lib/db.ts`. Wrap DB calls in try/catch and retries.
- For hashing to create deterministic IDs: use `crypto.createHash('sha256').update(url).digest('hex')`.
- Provide feature-flag `THECUEROOM_TEST_MODE=true` to:
  - Disable Playwright (use static HTML fixtures)
  - Use shorter TTLs
  - Use mocked HTTP responses in tests
- Provide a `/docs/FEED_FETCHING.md` that documents rate-limit policies, per-source TTLs, how to add a new source, and how to tune scheduler intervals.

Security & ethics
- Always respect robots.txt for scraping (unless admin override enabled).
- Include a `Crawl-Delay` and do not hammer servers. Default per-domain `minTime` 500ms; increase on 429.
- Provide clear logs when scraping is used (so you can contact site owners if needed).
- Never store or expose sensitive API keys in repo; use env vars for optional APIs (Spotify etc).

Testing requirements
- Unit test for `rssFetcher` handling 200 vs 304 vs 404.
- Unit test for `scraper` that uses provided selectors to parse sample HTML fixtures.
- E2E test for `/api/feeds` that runs in test-mode with mocked network (nock/msw).
- Performance check: simulate 200 sources with in-memory responses and verify queueing & rate limiting avoid exceeding concurrency limit.

CI
- Provide `.github/workflows/ci.yml` that runs `pnpm install && pnpm -w tsc --noEmit && pnpm test`.
- Ensure Playwright tests are marked optional in CI unless `ENABLE_E2E=true` in secrets.

Deliverables (exact files to output)
- `package.json` (root with pinned deps)
- `tsconfig.json`
- `src/*` files per structure above (full implementations)
- `config/spotlight-sources.ts` containing the full list you provided (convert to TypeScript Source[]).
- `README.md` with exact Replit & local commands and environment variables list.
- `.env.example`
- `tests/*` files
- `.github/workflows/ci.yml`

Extra: also create a small Next.js landing page (server-side) `src/pages/index.tsx` that calls `/api/feeds` server-side and renders the first page of items (title, excerpt, thumbnail) with lazy images and an `aria-live` region for updates. Use incremental static regeneration or server-side fetch with cache headers.

Acceptance test to run after generation in Replit:
1. `pnpm install`
2. set env `THECUEROOM_TEST_MODE=true` and `REFRESH_API_KEY=test-mode`
3. `pnpm dev`
4. Visit `http://localhost:3000/` — you should see the landing page rendered with feed items (or a clear notice for unreachable feeds).
5. Trigger a refresh for a source and observe `lastFetched` updated:
   ```bash
   curl -X POST -H "x-refresh-key: test-mode" "http://localhost:3000/api/refresh?sourceId=6am-group"
Run tests: pnpm test — all should pass in test-mode.

Now: generate the full repository in Replit with the above specification, using SQLite and the spotlight sources seeded. Produce all files, pinned package versions, and the test suite. Do not produce TODOs or placeholders — every file must be runnable.

pgsql
Copy code

---

If you’d like, I can now generate the **first set of files** from that Replit prompt here (package.json, tsconfig.json, .env.example, config/spotlight-sources.ts, src/lib/rssFetcher.ts, src/lib/scraper.ts, src/server/api/feeds.ts, tests for rssFetcher), ready for you to paste into a Replit project — tell me and I’ll spit them out as full file blocks.
::contentReference[oaicite:0]{index=0}





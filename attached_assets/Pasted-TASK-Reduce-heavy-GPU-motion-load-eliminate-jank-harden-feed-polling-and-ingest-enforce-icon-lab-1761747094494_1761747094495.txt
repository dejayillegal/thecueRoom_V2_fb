TASK: Reduce heavy GPU/motion load, eliminate jank, harden feed polling and ingest, enforce icon+label accessibility, implement robust AI/fallback flow, and make the whole monorepo fast and leak-free — **while preserving the app’s current dashboard style, theme, and layout everywhere except the landing page** (landing page may be redesigned). Implement, test, and commit with no TODOs.

IMPORTANT RULE (must be enforced in code changes)
- Preserve the exact dashboard style tokens, layout structure, color palette, spacing, and component placement across all internal pages and features (Dashboard, AI pages, Forum, Gigs, Profile, Admin, Mobile). Do **not** alter CSS variables, brand tokens, or the main layout components used by the dashboard pages.
- The landing page `/` is the only place allowed to receive a redesigned layout/visuals.

GOALS (priority order)
1. Keep dashboard/theme/layout unchanged across internal pages (verify by snapshot tests).
2. Remove or disable heavy GPU-intensive visual effects globally (except landing page where redesign may use heavier visuals if desired).
3. Ensure every clickable control has icon + visible label.
4. Centralize and use one constant fallback thumbnail for feeds.
5. Make style presets visible only when SVG fallback is active (no AI key); otherwise hide presets and enable prompt input.
6. Robust, configurable feed poller for admins; non-blocking, retriable, and self-healing.
7. Fix safe JSON handling to avoid `response.json()` on HTML responses.
8. Workerize heavy tasks (AI rasterize, sharp) and limit concurrency.
9. Add tests: typecheck, unit, e2e (scroll/perf), and perf diagnostics.
10. Commit with clear message and produce a report.

FILES / ACTIONS (must implement these exact or equivalent)
- Global safety & style:
  - `apps/web/app/globals.css` — remove/replace heavy CSS where used globally but preserve root tokens and theme variables used by dashboard components. Add `.reduced-motion` rule and prefer `opacity`/`transform` only.
  - Ensure `--background`, `--surface`, `--tcr-accent`, `--primary`, and other brand tokens remain exactly as in theproject preamble.

- Icon+label standard:
  - `apps/web/src/components/ui/IconButton.tsx` — new canonical Icon+Label button. Replace all icon-only instances across app with this.
  - Run a repo-wide replacement of icon-only usages in navigation, cards, toolbars, modals, and action menus.

- Feed thumbnail centralization:
  - `apps/web/public/fallback-thumbnail.png` — add constant thumbnail.
  - `apps/web/src/lib/feed-image.ts` — always return fallback-thumbnail when article image missing or invalid. Remove any code that renders title-as-image.

- AI env & presets:
  - `apps/web/app/api/ai/env/route.ts` — returns `{ hasAIKey: boolean }`.
  - `apps/web/app/api/ai/generate/route.ts` — AI-first; fallback to SVG if no key or failures. Always return valid JSON.
  - `packages/ai/impl/fallback-svg.ts` — add all presets (Neon Accent, Monochrome, Geometric, Brutalist, Cybergrind, Vaporwave, Chromatic Grid, Noir Light, Acid Geometry, Liquid Metal). Ensure visually polished randomization.
  - `apps/web/src/components/AI/CoverArtStudio.tsx` — show style presets only when `hasAIKey===false`. If AI key present, show prompt input enabled and hide presets. Add Randomize button for fallback mode.

- Poller & Admin:
  - `packages/feeds/poller.ts` (or `apps/web/packages/feeds/poller.ts`) — implement robust poller with:
    - Admin-configurable `poll_interval_seconds`, `poll_concurrency`, `failure_threshold`.
    - AbortController, per-fetch timeout, exponential backoff, disable-on-repeated-failure behavior.
    - Runs in worker thread / child_process to avoid blocking main thread.
  - `apps/web/app/(admin)/feed-settings/page.tsx` — admin UI to adjust poller settings and toggle sources. Must use same dashboard layout and theme.
  - `apps/web/app/api/admin/feed-poller/route.ts` — read/write poller settings (protected; in dev mock admin auth).

- Safe JSON & fetcher:
  - `apps/web/src/lib/safe-fetch.ts` — `safeFetch` + `safeJsonResponse` helpers. Replace all `fetch(...).json()` in repo with safe patterns.
  - Ensure all API routes and external fetches never return HTML to client; when HTML is returned, server returns structured JSON `{ ok:false, status, snippet }`.

- Performance & memory:
  - `apps/web/src/hooks/use-debounce.ts`, `use-safe-interval.ts`, `use-event-listener.ts` — ensure cleanup and no leaks.
  - `apps/web/src/components/VirtualList.tsx` — ensure long lists (feeds, forum) are virtualized with `react-virtual` or similar.
  - Move heavy rasterization/AI tasks to `packages/ai/worker.js` (child_process.fork or worker_threads), limit concurrency by `AI_WORKER_CONCURRENCY` env var.

- Diagnostics & tests:
  - `scripts/diag/check-heavy-css.js` — scan for banned properties (`backdrop-filter`, large `filter: blur`, heavy `box-shadow`, `mix-blend-mode`).
  - `scripts/diag/run-scroll-stress.js` — headless scroll stress test using Playwright/Puppeteer.
  - `scripts/diag/check-memory.js` — spawn 3 TEST_MODE jobs and assert reasonable heap growth.
  - `tests/perf/scroll.test.ts` — automated test integrating with stress script.
  - Add snapshot tests to verify that dashboard pages’ HTML/CSS classnames/layout remain unchanged after modifications (landing page excluded).

- Fallback thumbnail usage:
  - `apps/web/src/components/FeedItem.tsx` — use `ImageWithFallback` which uses centralized feed-image logic.

- Landing page exception:
  - Only `apps/web/app/page.tsx` (landing page) may be altered visually. Ensure any performance/motion reductions applied globally do not prevent landing page designers from using heavier visuals if desired — but make them optional via admin/perf toggle.

ENV VARS (add/update `.env.example`)
- HF_TOKEN=                # optional
- AI_TEMP_DIR=/tmp/thecueroom-ai
- AI_WORKER_CONCURRENCY=1
- POLL_INTERVAL_SECONDS=60
- POLL_CONCURRENCY=3
- FEED_FAILURE_THRESHOLD=5
- TEST_MODE=false

DEPENDENCIES (exact commands)
- pnpm --filter apps/web add sharp zod cross-fetch nanoid react-virtual p-limit
- pnpm --filter apps/web add -D vitest playwright axe-core

ACCEPTANCE CHECKS (must be run and pass)
1. Typecheck:
   - `pnpm -w tsc --noEmit` → 0 errors
2. Unit tests / snapshots:
   - `pnpm --filter apps/web test:unit` → all tests pass
   - Dashboard snapshot tests confirm no layout changes for dashboard routes (e.g., `/dashboard`, `/dashboard/settings`, `/dashboard/gigs`)
3. Scroll stress:
   - `node scripts/diag/run-scroll-stress.js http://localhost:3000/feed` → pass: frame drops ≤ 5%, no main-thread blockage >100ms
4. CSS scan:
   - `node scripts/diag/check-heavy-css.js` → zero banned properties in dashboard CSS (landing page allowed exceptions logged)
5. Feed poller simulation:
   - `TEST_MODE=true node packages/feeds/poller.js --simulate 50 --duration 15` → must respect concurrency, disable failing sources after threshold, and not crash
6. Memory:
   - `node scripts/diag/check-memory.js` → heapUsed growth acceptable (<50MB across test)
7. UI behavior:
   - CoverArtStudio: when `/api/ai/env` returns `{ hasAIKey:false }`, presets are visible and prompt disabled. When true, presets hidden and prompt enabled.
   - All interactive elements across dashboard pages show icon + visible label.
   - Missing feed images use `/fallback-thumbnail.png`.

REPORT OUTPUT (assistant must produce)
- List of all changed files (git show --name-status HEAD)
- Outputs of:
  - `pnpm -w tsc --noEmit`
  - `pnpm --filter apps/web test:unit`
  - `node scripts/diag/check-heavy-css.js`
  - `node scripts/diag/run-scroll-stress.js` summary
  - `node scripts/diag/check-memory.js` summary
- Snapshot diff summary proving dashboard layouts unchanged.
- `git commit` sha and commit message.

COMMIT MESSAGE
- `perf(ui): reduce heavy animations, workerize ai, centralized fallback thumbnail, robust poller, preserve dashboard layout`

IMPLEMENTATION NOTES / RULES
- Do not modify brand tokens or the main dashboard layout components.
- Where a CSS heavy property exists only on landing page, it may remain; ensure `check-heavy-css.js` reports allowed landing page exceptions.
- All new code must include JSDoc comments and types.
- If native modules (sharp) fail to install in Replit, implement TEST_MODE path that uses pure SVG rasterization with `svg2png` JS fallback and document steps to enable native later.
- No TODOs in committed code. Tests must pass or the assistant must auto-revert and report precisely.

DO NOT ASK ANY QUESTIONS — implement, run tests, commit, and produce the full report above. If any step cannot be completed due to platform constraints, provide a deterministic fallback plan and exact commands the user can run locally to finish.

END OF PROMPT

Task: Fix and fully optimize the thecueRoom **Dashboard** page. Perform a professional code cleanup and ship production-ready changes. The assistant should write code, run tests, fix failures, and commit the final result.

High-level goals (do all automatically):
- Replace the current Dashboard UI with a polished, high-performance implementation that matches the project's dark/neon theme and uses the canonical logo at /apps/web/public/logo.svg (do NOT modify the logo).
- Remove memory leaks, dangling timers, and duplicated event listeners related to the dashboard.
- Eliminate scroll jank and improve smoothness; virtualize long lists.
- Debounce search & filter inputs (300ms) and prevent reflows during typing.
- Lazy-load and progressively load all thumbnails; implement a robust ImageWithFallback component.
- Implement Spotlight column vertical auto-scroll with requestAnimationFrame; respect prefers-reduced-motion and pause on hover.
- Centralize and clean navigation for dashboard links to avoid full reloads; use Next Link/router.replace consistently.
- Minimize bundle + runtime cost for the dashboard by code-splitting page sections (lazy-load less-critical widgets).
- Add unit tests and a simple perf smoke test. Run typechecks and tests and fix any issues until green.
- Commit changes with message: `fix(dashboard): performance, memory-leak cleanup, virtualization, image-fallback, spotlight-raf`.

Constraints & rules:
- Keep TypeScript strictness (tsconfig strict mode).
- No secrets in repo; code must run in TEST_MODE when env missing.
- Pin any new npm packages to exact versions in package.json.
- Do not change routes outside the dashboard scope.
- Keep UI consistent with existing dashboard tokens (`--tcr-accent` lime, `--surface` #111111, background #0B0B0B).
- All new code must include JSDoc/type annotations and Zod validation for any API inputs.

Files to create/modify (assistant decides exact implementation, but must touch these paths):
- apps/web/src/app/dashboard/page.tsx (replace/clean; use DashboardLayout wrapper)
- apps/web/src/components/DashboardLayout.tsx (new: Sidebar + Header + main container)
- apps/web/src/components/Sidebar.tsx (memoize, clean Link usage)
- apps/web/src/components/Header.tsx (debounced search -> router.replace)
- apps/web/src/components/Dashboard/TopBanner.tsx (lazy-loaded)
- apps/web/src/components/Dashboard/GigRadar.tsx (virtualized list if >10 items)
- apps/web/src/components/Dashboard/SpotlightColumn.tsx (RAF auto-scroll)
- apps/web/src/components/Dashboard/RecentActivity.tsx (virtualized)
- apps/web/src/components/ImageWithFallback.tsx (progressive, lazy, onError fallback)
- apps/web/src/components/VirtualList.tsx (lightweight virtualizer or pinned react-window usage)
- apps/web/src/hooks/use-debounce.ts
- apps/web/src/hooks/use-safe-interval.ts
- apps/web/src/hooks/use-event-listener.ts
- apps/web/src/hooks/use-shared-socket.ts (singleton pattern)
- apps/web/src/lib/fetcher.ts (AbortController + timeout)
- apps/web/app/globals.css (ensure consistent background, GPU-safe animations)
- tests/dashboard.unit.test.ts (Vitest unit test for Dashboard render + key interactive flows)
- scripts/diag/check-memory.js (small script to detect active timers / sockets in TEST_MODE)

Acceptance criteria (assistant must run and paste outputs in final message):
1. Typecheck: `pnpm -w tsc --noEmit` must exit 0. Paste the output summary.
2. Unit tests: `pnpm --filter apps/web test:unit` (or `pnpm --filter apps/web vitest`) must pass. Paste test summary.
3. Memory check: run `node scripts/diag/check-memory.js` after mounting/unmounting dashboard components in test mode and report zero leaked timers or sockets.
4. Local dev sanity: start web dev server and confirm `GET /dashboard` returns HTTP 200 and the hero/logo is visible. Include a short screenshot or text description of the DOM check (logo, search, spotlight).
5. Performance smoke: run `pnpm --filter apps/web perf:baseline` (or run a quick Lighthouse performance audit) and paste the resulting performance numbers (or a brief summary: main-thread blocking time, Total Blocking Time). The assistant should aim to measurably reduce main-thread work caused by the previous implementation.

Behavioral test cases assistant must enforce in code:
- Navigation links in Sidebar and TopBanner must use `<Link>` for internal navigation (no full page reload).
- Search input: typing should not cause layout reflow; network navigation is debounced 300ms and uses `router.replace` so Back button isn’t spammed.
- Long lists (Recent Activity, Gig Radar if >10 items) must use VirtualList to keep DOM node count low.
- All setInterval/setTimeout usage must be wrapped in use-safe-interval with cleanup; any event listeners must use use-event-listener for auto-cleanup.
- SpotlightColumn auto-scroll must use requestAnimationFrame and be canceled on unmount; it must pause on hover/focus and respect prefers-reduced-motion.
- ImageWithFallback must use lazy loading, have width/height to avoid CLS, and swap to fallback on error.

Testing & CI touches:
- Add or update apps/web/package.json scripts:
  - "test:unit": "vitest"
  - "perf:baseline": node scripts/diag/run-lighthouse-local.js http://localhost:3000 (or appropriate)
  - "check:leaks": node scripts/diag/check-memory.js
- If tests fail, fix until green.
- Make a single commit with the required message and include the output of `git show --name-status HEAD` in the final report.

Deliverables in assistant final message (must include):
- Confirmation that all acceptance checks passed OR failing logs if not.
- Outputs of the commands run (typecheck + unit tests + memory check + perf).
- `git show --name-status HEAD` output.
- List of files created/modified with exact paths.
- Brief 3–5 line summary of what caused the previous memory/scroll issues and what was fixed.

Do not ask questions — implement, test, commit, and report. Respect the project constraints and the canonical logo file. Run the commands below and paste outputs in the final message:

Commands the assistant must run:
- `pnpm -w install`
- `pnpm -w tsc --noEmit`
- `pnpm --filter apps/web test:unit`
- `node scripts/diag/check-memory.js`
- `pnpm --filter apps/web perf:baseline`
- `git add -A && git commit -m "fix(dashboard): performance, memory-leak cleanup, virtualization, image-fallback, spotlight-raf" && git show --name-status HEAD`


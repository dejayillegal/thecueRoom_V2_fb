Top-level goals (priority):

Clicking a thread immediately navigates to full thread view (/community/thread/[id]) and shows replies.

Thread list shows a skeleton / spinner while loading — never show “No threads yet” until finished loading and empty.

Replies: allow posting replies inline, optimistic UI, server-side validation, and fallback if moderation blocks.

Likes: toggle like/unlike with optimistic update and debounce to avoid spamming API.

Virtualize thread list and replies to support large threads without lag.

Cache API responses short-term and show near-instant UI from cache while background-refreshing.

Fix contributor panel styles: font-size reduced by ~1.5 steps (use existing tokens; do NOT change brand tokens).

Add unit & e2e tests for thread click → view → reply → like flow.

Run diagnostics and produce a short verification report.

Files & exact edits to implement (implement code, no TODOs):

A. API & backend sanity

Verify these routes exist & behave:

GET /api/forum/threads?cursor=&limit=20&category=... → returns { items: Thread[], nextCursor?: string }

GET /api/forum/thread/[id] → returns { thread, replies: Reply[], meta }

POST /api/forum/thread/[id]/reply → accepts { body }, returns created reply or review status

POST /api/forum/thread/[id]/like → toggles like; returns { liked: boolean, likesCount }

If any route returns HTML on error, wrap with JSON error: { ok:false, status, message, snippet }

Ensure server pagination and ETag or short TTL caching headers for thread lists.

B. Client: thread list (virtualized + safe UX)

File: apps/web/src/app/community/forum/page.tsx (or wherever main forum page lives)

Replace current list rendering with a virtualized list (react-virtual or react-window) for threads.

Use a useThreads hook that:

calls safeFetch('/api/forum/threads?...') (see safeFetch pattern).

returns { data, loading, error, refresh, loadMore }.

implements cache-first + background refresh: if cached data exists, return it immediately and trigger a background fetch to update.

exposes an isIdle / isStale flag.

UX:

On first render show skeleton cards (same card dimensions) while loading.

DO NOT show “No threads yet” until loading === false AND data.items.length === 0.

Each thread card must be a semantic <a href="/community/thread/[id]" role="button"> (use Next.js Link) to permit right-click/open-in-new-tab.

Card clicks should navigate to /community/thread/[id]?focus=true and also open via client-side router push for single-page nav.

Preload thread detail: when user hovers or focuses a thread card for >150ms, prefetch GET /api/forum/thread/[id] and prefetch the thread page chunk to reduce latency.

C. Client: thread page (replies, likes)

File: apps/web/src/app/community/thread/[id]/page.tsx (or component)

Use virtualization for the replies list (if > 20 replies).

Use useThread hook that:

fetches thread + first-page replies

exposes postReply(body) and toggleLike() functions

uses optimistic updates: when posting a reply, immediately append a "pending" reply with id: temp-<uuid> and status: pending; on server success, replace; on failure, mark failed and show retry.

For like toggles: update likesCount and liked locally immediately, then POST. If server returns error, revert and show toast.

Use AbortController for fetches; cancel when unmounting.

Debounce like toggles (200ms) and coalesce repeated clicks.

Replies should run through moderation endpoint before posting (server does it), but on client show clearly if reply is under review.

D. Safe fetch / error handling

Use existing safeFetch helper; if duplicate declarations exist (build error), fix by consolidating into src/lib/safe-fetch.ts and export a single function.

safeFetch(url, { attempts=?, timeout=?, headers? }) must:

Attempt retry with exponential backoff

Return parsed JSON when possible. If response not JSON, return structured error and avoid calling res.json() directly.

Respect signal for cancellation.

E. Optimistic UI & offline feel

Use nanoid() for temporary ids.

Show moderation states:

pending (shaed translucent card)

failed (red toast + retry)

review (gray label; show “Under review by AI”)

Persist unsent replies to localStorage in case of reload.

F. Performance & virtualization

Add virtualization component apps/web/src/components/VirtualizedList.tsx using react-virtual.

Use windowing for both threads and replies; ensure itemSize accurate and use dynamic measurement for variable height (or estimate and gradually measure).

G. CSS / Visual tweaks

Contributor panel font size: edit apps/web/src/components/Sidebar/ContributorPanel.tsx (or wherever):

reduce font-size by calc(var(--text-sm) - 1.5px) or equivalent token usage (text-sm -> text-xs but preserve tokens).

Ensure avatar + badges scale properly. Do NOT modify root brand tokens.

Add focus / hover states for thread cards (neon border) but ensure prefers-reduced-motion respected.

H. Tests

Add / update tests:

tests/forum/thread-click.test.ts (Playwright or Vitest + jsdom): click thread card navigates to thread page and loads replies.

tests/forum/reply-like.test.ts (unit): posting a reply performs optimistic update and finalizes after server returns.

tests/forum/no-false-empty.test.ts: page should show skeleton while loading and not the “No events yet” message.

Add a diagnostic script scripts/diag/forum-smoke.js that:

starts a local server (if needed)

fetches /api/forum/threads?limit=3, ensures response shape, then programmatically navigate to first thread, post a test reply (TEST_MODE) and toggle like.

I. Logging & metrics

Add console logs for dev with clear tags: [Forum] Prefetch thread: id, [Forum] Reply optimistic: temp-....

Record timing for thread detail fetch and show in diagnostics.

J. Accessibility

Ensure thread cards and composer elements have proper aria-* attributes.

Ensure keyboard navigation: Enter on focused card opens thread, ESC closes modals.

K. Acceptance checks to run (assistant should run these on completion and paste outputs)

Typecheck: pnpm -w tsc --noEmit → 0 errors

Unit tests: pnpm --filter apps/web test:unit → all pass

E2E smoke: node scripts/diag/forum-smoke.js → print pass/fail

Manual check: open /community/forum, click a thread, verify thread loads within 500ms (when cached) or <2s cold, can post reply, can like/unlike.

Visual check: contributor panel font reduced by ~1.5; show screenshot.

Output required from Replit Assistant:

Short summary of exact files changed (git show --name-status HEAD)

Results of the acceptance checks above

One screenshot of /community/forum thread open with reply composer visible

One screenshot of contributor panel showing reduced font size

If anything cannot be run in Replit (e.g., Playwright not allowed), explain deterministic local steps to run tests.

Implementation constraints:

Preserve all brand tokens and dashboard layout components. Only change contributor font-size and forum-specific CSS.

Use existing project utilities (safeFetch, analytics perf marks) where available; do not introduce conflicting duplicate helpers.

No TODOs in committed code.

Keep memory usage low: workerize heavy jobs if needed (but for this ticket focus on client rendering & API behavior).

Commit message suggestion:

fix(forum): clickable thread navigation, optimistic replies & likes, virtualized lists, safe-fetch consolidation, contributor font tweak
1) Quick investigation & diagnostic endpoints (first, low-risk)

Create temporary debug endpoints and logs so we can see what the server returns and what the client is getting.

Add apps/web/app/api/debug/gigs-payload/route.ts — a GET that returns:

the cached payload (if cache exists) and

the last raw source logs for each provider (tail 30 lines).
Return JSON: { cacheAgeSec, fromCache, eventsCount, sampleEvent, sources: {name: { lastAttemptMs, method, statusCode, lastError }} }

Add a client-side debug toggle:

localStorage.setItem('TCR_DEBUG_SHOW_RAW_GIGS', '1') will show raw server payload at top of gigs page.

This gives immediate visibility.

2) Server: ensure /api/gigs/india returns a consistent normalized shape

Open apps/web/app/api/gigs/india/route.ts and do the following:

Ensure the route ALWAYS returns JSON in this exact shape:

{
  ok: true,
  fromCache: boolean,
  cacheAgeSeconds: number|null,
  meta: {
    totalSources: number,
    sources: Array<{ name:string, items:number, method:string, status:string, durationMs:number }>
  },
  events: NormalizedEvent[]   // see NormalizedEvent shape below
}


If an error occurs, return ok:false, error:{ message, code } with a 200 HTTP status for UI stability.

NormalizedEvent definition (enforce):

{
  id: string,
  title: string,
  start: string, // ISO
  end?: string,
  venue?: string,
  city?: string,
  url?: string,
  ticketUrl?: string,
  source: string,
  raw?: any
}


Add structured telemetry for each source attempt:

method chosen (rss|json|ld|xhr|sitemap|headless),

durationMs,

statusCode,

itemCount,

error (if any).
Write these into logs/feeds/diagnostics.jsonl as newline JSON lines.

Fix common bug: ensure safeFetch is imported from a single canonical helper (src/lib/safe-fetch.ts). If there are multiple safeFetch declarations in the repo, delete duplicates and consolidate.

AbortController: In safeFetch, create a NEW AbortController() for every attempt. Use:

const controller = new AbortController();
const id = setTimeout(()=> controller.abort(), timeout);
try {
  const res = await fetch(url, { ...options, signal: controller.signal });
  ...
} finally {
  clearTimeout(id);
}


Do not reuse the controller object across attempts. Add try/catch to avoid Controller is already closed unhandled exceptions.

3) Poller & headless worker fixes

Ensure packages/feeds/poller.ts:

Uses p-limit for concurrency.

Writes the normalized events to the shared cache (packages/feeds/cache.ts) and to an on-disk JSON file data/gigs/india-latest.json (atomic write: write to tmp then rename).

If headless rendering is required, call a worker process packages/feeds/headless-worker.js via child_process.fork() (do not import Playwright in any module that is included in the client bundle).

The worker should not be imported by Next bundler. Use dynamic spawn/fork only from server-only code.

packages/feeds/headless-worker.js:

If Playwright is not installed or not allowed in Replit, gracefully fallback to cheerio parsing and mark headless_available:false in diagnostics.

Do not cause Next's webpack to attempt bundling Playwright; keep it in a separate node script.

Add env flags in .env.example:

PLAYWRIGHT_ENABLED=false
PLAYWRIGHT_HEADLESS=false
POLL_CONCURRENCY=3
FEEDS_CACHE_TTL_SECONDS=600

4) Fix BookMyShow / 403 handling and proxy recommendation

In packages/feeds/sources/bookmyshowIndia.ts:

If you receive 403, log it with blocked:true and do NOT retry aggressively. Instead mark the source status: "blocked_403" in meta.

Add admin setting to toggle useProxyForBookMyShow. If SCRAPING_PROXY_URL is set and useProxyForBookMyShow true, route the fetch through the proxy.

Add admin UI (under /app/(admin)/feed-settings/page.tsx) to:

Toggle useProxy per-source,

Force headless per-source,

Run "Run Now" per-source.

Include a big notice in the admin UI: BookMyShow often blocks cloud IPs; if 403 persists, use your own VPS scraper or a proxy under your control.

5) Client UI fixes — gigs page

Open apps/web/src/app/gigs/page.tsx or the gigs component:

Add a robust fetch routine to the client that expects the canonical shape above.

Very likely root cause: client-side filters are hiding events. Check the client filter logic:

City filter default should be "All Cities" and should not filter anything if set to that.

Date filter: ensure timezone parsing is correct — parse start using new Date(event.start) and compare to the user timezone; do not drop events accidentally due to NaN or string comparisons.

Implement defensive checks: if events exists but events.length === 0 and meta.totalSources > 0, show “Updating events — serving cached results” and include a Show raw payload debug button (enabled when TCR_DEBUG_SHOW_RAW_GIGS=1).

Replace any client-side code that assumes events is nested under data.items or similar — follow canonical response.events.

Add a fallback rendering mode: when debug flag TCR_DEBUG_SHOW_RAW_GIGS is set, render the JSON payload in a collapsible <pre> at top so you can visually confirm the server payload.

Ensure image loading & fallback: images use ImageWithFallback and display /public/fallback-thumbnail.png when missing.

6) Tests & diagnostics (must run after code edits)

Add or update the following scripts:

scripts/diag/gigs-poller-once.js — run poller once (server-side), log per-source diagnostics and write data/gigs/india-latest.json.

scripts/diag/check-gigs-ui.js — simple Node fetch of http://localhost:3000/api/gigs/india and check:

ok === true

events is array and events.length > 0

Print meta.sources summary

Add in package.json (apps/web):

"scripts": {
  "diag:gigs-once": "node scripts/diag/gigs-poller-once.js",
  "diag:gigs-ui": "node scripts/diag/check-gigs-ui.js"
}


Acceptance checks (assistant must run these and paste outputs in the final report):

pnpm -w tsc --noEmit → 0 errors

node scripts/diag/gigs-poller-once.js → logs showing each source, method, status, and itemCount. At least one source must return itemCount > 0 (RollingStone or Zomato qualifies).

curl http://localhost:3000/api/gigs/india | jq showing ok:true, events.length > 0, meta.sources present.

Load the gigs page in the running app and confirm it shows events (not “No events found”). If it still shows no events, the assistant must turn on TCR_DEBUG_SHOW_RAW_GIGS and provide the payload.

7) Root cause checks to run & fix if found

Duplicate safeFetch:

If src/lib/safe-fetch.ts contains multiple export function safeFetch declarations or duplicate files (search repo), consolidate into a single helper and change imports.

Playwright bundling error:

Ensure Playwright code is only required in packages/feeds/headless-worker.js and never imported by modules under apps/web/src so webpack doesn’t attempt to bundle electron or Playwright. If playwright is present in code imported by Next pages, move it to the worker.

AbortController:

Replace any code that reuses an AbortController instance; create new one per request.

8) Fast temporary workaround (so the UI shows events now)

If the full poller fix will take time, implement a short-lived patch:

On server route /api/gigs/india — if the canonical data/gigs/india-latest.json exists, return it and set fromCache:true. That file can be written by your existing successful Rolling Stone fetch. This makes the UI show events immediately while we harden the rest.

Add this logic first — it’s safe and non-breaking.

9) Final report items (assistant must include)

After implementing, run checks and return a compact report including:

git show --name-status HEAD (list changed files)

Output of pnpm -w tsc --noEmit

Output of node scripts/diag/gigs-poller-once.js

Output of curl http://localhost:3000/api/gigs/india | jq

Screenshot capture instructions of /gigs/india showing events (or include paste of the small list)

If any source is blocked (BookMyShow 403), include logs/feeds/bookmyshow.log tail and recommended next step (enable proxy or run poller from VPS) and how to set SCRAPING_PROXY_URL.

Extra: Safety & ethics note to display in admin UI

Add a short statement in admin feed-settings: “We attempt to fetch publicly-available event data. Scraping can violate some sites’ terms. Use official APIs or ask permission. For blocked sources you may run an internal poller from a small VPS.”

Environment changes to make (add to .env.example)
PLAYWRIGHT_ENABLED=false
PLAYWRIGHT_HEADLESS=false
SCRAPING_PROXY_URL=
POLL_CONCURRENCY=3
FEEDS_CACHE_TTL_SECONDS=600

Final instruction to Replit Assistant

Implement all code changes above.

Run acceptance checks (TypeScript, poller-once, curl).

If UI still shows “No events found,” enable TCR_DEBUG_SHOW_RAW_GIGS=1 and paste the server payload and client console logs so I can parse them.

If BookMyShow remains 403, enable admin useProxy toggle and show exact proxy test curl that works (the assistant should not set a real third-party paid proxy — just show how to configure one).
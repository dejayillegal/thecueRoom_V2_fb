theCueRoom V2 — Platform-Agnostic, Free-Tier-Only, Provider-Flexible Refactor

Project: theCueRoom V2
Stack (MUST NOT CHANGE):

Next.js 15

pnpm

Drizzle ORM

Supabase (default)

Primary Goal:
Remove all Replit dependencies, make the project portable to any platform, add a future-proof provider abstraction, support a Supabase → Neon migration path, introduce a one-command setup, and create detailed, platform-specific setup documentation — using ONLY FREE TIERS.

⚠️ ABSOLUTE RULE:
Do NOT break existing functionality, schemas, scripts, or architecture.
All changes must be additive, isolated, and backward-compatible.

1️⃣ REMOVE ALL REPLIT DEPENDENCIES (MANDATORY)
You must:

Remove all Replit-specific assumptions, including:

Scheduled Deployments

Replit secrets

Replit URLs

Replit filesystem paths

Replit port assumptions

Replace with standard Node.js behavior

Server rules:

Bind to 0.0.0.0

Use process.env.PORT || 5000

Never hardcode hostnames or ports

Result:

The app must run identically on:

Local machine

Docker

VPS

Vercel (free)

Railway (free tier)

Fly.io (free tier)

2️⃣ FUTURE-PROOF PROVIDER ABSTRACTION (DO NOT BREAK DB)
Goal

Allow switching databases without touching application logic.

Constraints

Supabase remains default

Drizzle schema & migrations remain unchanged

All existing imports must continue to work

Implement:
packages/db/
├── config.ts
├── providers/
│   ├── supabase.ts
│   ├── neon.ts
│   └── index.ts

config.ts

Reads:

DB_PROVIDER=supabase | neon

Provider adapters

supabase.ts: existing Supabase Postgres connection

neon.ts: Neon Postgres via @neondatabase/serverless

index.ts: runtime provider selector

🚫 Do NOT change query usage anywhere else in the app.

3️⃣ SUPABASE → NEON MIGRATION PATH (SAFE & OPTIONAL)
Migration rules

No schema changes

No migration rewrites

Drizzle migrations must run unchanged

Required documentation

Explain clearly:

Supabase = DB + Auth (default)

Neon = DB only

Hybrid mode allowed:

Neon DB + Supabase Auth (free tiers)

Migration steps must be documented:
DB_PROVIDER=neon
DATABASE_URL=postgres://...
pnpm migrate

4️⃣ ONE-COMMAND SETUP (MANDATORY)
Add:
scripts/setup.ts

pnpm setup must:

Validate .env

Check DB connectivity

Run migrations

Seed admin user

Seed news sources

Run initial feed ingestion

Rules:

Idempotent

OS-agnostic

No platform assumptions

No paid services

5️⃣ UNIVERSAL CRON (FREE-ONLY)
Requirements

Keep /api/cron/ingest

Secure with CRON_SECRET

Must work with FREE cron options only:

cron-job.org (free)

GitHub Actions (free)

system cron (free VPS)

Fly.io cron (free tier)

CLI fallback:
pnpm ingest

6️⃣ DEPLOYMENT MATRIX (FREE TIERS ONLY)
You MUST document each platform separately.
Platforms (ALL FREE):
Platform	Database	Cron	Notes
Local	Supabase / Neon	system cron	Dev
Vercel	Supabase / Neon	cron-job.org	Frontend
Railway	Supabase / Neon	Built-in	Full-stack
Fly.io	Supabase / Neon	Fly cron	Edge
VPS (Ubuntu)	Supabase / Neon	systemd cron	DIY

🚫 No paid plans
🚫 No enterprise features
🚫 No vendor lock-in code

7️⃣ DOCUMENTATION (CRITICAL — MUST CREATE FILES)
You MUST update and/or create:
README.md

Platform-agnostic overview

One-command setup

Provider switching

Security checklist

Production warnings

/docs/ (REQUIRED)

Create separate, detailed docs:

docs/
├── LOCAL_SETUP.md
├── VERCEL_DEPLOY.md
├── RAILWAY_DEPLOY.md
├── FLY_DEPLOY.md
├── VPS_DEPLOY.md
├── CRON_SETUP.md
├── PROVIDERS.md


Each doc must include:

Free-tier requirements

Environment variables

Step-by-step commands

Common failure cases

How to verify success

8️⃣ ENVIRONMENT VARIABLES (STANDARDIZED)
Update .env.example

Fully commented

Provider-aware

No platform-specific values

Must include:

DB_PROVIDER=
DATABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
CRON_SECRET=
ADMIN_EMAIL=
ADMIN_PASSWORD_HASH=

9️⃣ QUALITY GUARANTEES (NON-NEGOTIABLE)

Do NOT break:

Feed ingestion

Admin console

Auth

AI adapters

UI

No schema changes

No forced migrations

No new paid dependencies

Keep folder structure intact

Add comments only where abstraction is non-obvious

📦 FINAL DELIVERABLES

Provider abstraction layer

Neon support (optional, documented)

One-command setup script

Universal cron system

Platform-specific FREE setup docs

Clean, Replit-free README

Updated .env.example

If any step would introduce a breaking change, stop and choose the non-breaking alternative.
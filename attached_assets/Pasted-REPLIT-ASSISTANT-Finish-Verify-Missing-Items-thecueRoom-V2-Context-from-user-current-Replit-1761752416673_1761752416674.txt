REPLIT ASSISTANT: Finish & Verify Missing Items (thecueRoom V2)

Context (from user / current Replit summary)
- Repo: thecueRoom V2 (monorepo) — server running on :5000, worker running
- Implemented: AI SVG presets, feed poller, IconLabelButton, fallback-thumbnail.svg, safe JSON, basic AI endpoints.
- Current issues noted:
  - 28 TypeScript type mismatches (non-blocking for now)
  - Diagnostic scripts require `glob` at root
  - Need stronger tests, snapshots & e2e/perf checks
  - Ensure compose/text+watermark server-side and client-side flows are implemented and tested
  - Ensure sharp fallback & TEST_MODE safe paths work in Replit
  - Ensure fonts and watermark assets exist and are used
  - Ensure centralized fallback thumbnail is a PNG (not just SVG) and used everywhere
  - Run and report the acceptance checklist from the previous master prompt

Goal for this run
A thorough finishing pass: find and implement the missing items from the prior 3 prompts, fix TS errors or explicitly document exemptions, run diagnostics & performance tests, finalize compose (auto-stamp + editor) flows, ensure robustness in Replit (sharp fallback), and produce a full report and commit.

Do all of the following steps now. Do not ask questions.

PHASE 0 — quick verification (do first)
1. Print a compact environment summary:
   - `node -v && pnpm -v && uname -a`
   - Show ports in use (grep for 5000).
2. Show git HEAD: `git rev-parse --short HEAD && git show --name-status -1`

PHASE 1 — install small dev deps & fix immediate script missing dep
1. Ensure `glob` is present for diag scripts:
   - Run: `pnpm -w add -D glob`
2. If `sharp` is installed already, note it; if not, attempt to install and detect failure:
   - `pnpm --filter apps/web add sharp || echo "sharp_install_failed"`
   - If install fails, set a repo-level file `dev-data/SHARP_BUILD_FAILED` and switch workers to `@svg2png-js` fallback paths.

PHASE 2 — TypeScript hygiene & CI-ready checks
1. Run TypeScript check:
   - `pnpm -w tsc --noEmit`
2. Where there are errors:
   - If errors are from third-party types or peer-deps, fix by either:
     a) Adding explicit type packages (e.g., `@types/*`) and updating tsconfig "paths", **or**
     b) Add short, local declaration files under `types/` to silence false positives (allowed only when safe).
   - Do not silence real runtime errors; fix them.
3. After fixes, `pnpm -w tsc --noEmit` must return 0 errors. If you cannot get to 0 due to environment limits, produce a short list of remaining errors with exact files/lines and mark the run as partial (but try to fix as many as possible).

PHASE 3 — Compose (text + watermark) — finish and test
Ensure these endpoints + worker compose job exist and are robust:

A. Server endpoints — if any are missing, create them:
- /apps/web/app/api/ai/compose/route.ts
- /apps/web/app/api/ai/compose/status/[composeJobId]/route.ts
- /apps/web/app/api/ai/job/[jobId]/route.ts  (confirm exists)
- /apps/web/app/api/ai/generate/route.ts      (confirm robust fallback)
- /apps/web/app/api/ai/env/route.ts           (confirm exposes hasAIKey)

B. Worker compose support:
- packages/ai/worker.js (or .ts)
  - Add job type `composeTextWatermark`
  - Preferred path: use `sharp` to composite an on-the-fly SVG overlay onto base PNG (stream to disk).
  - Fallback path: if sharp missing, produce full SVG composition then rasterize with `@svg2png-js` (stream when possible).
  - Respect `AI_WORKER_CONCURRENCY` and `AI_TEMP_DIR`.
  - Write status metadata `AI_TEMP_DIR/<composeJobId>.json` and image `AI_TEMP_DIR/<composeJobId>.png`.

C. Fonts & watermark assets:
- Ensure fonts included:
  - apps/web/public/fonts/Inter-Variable.woff2
  - apps/web/public/fonts/SourceCodePro-Regular.woff2
- Ensure watermark icon: apps/web/public/icons/watermark-small.svg
- If not present, create minimal licensed-free fonts (e.g., Google Inter subset) or note fallback to system fonts.

D. Client side compose flows:
- apps/web/src/components/AI/CoverArtStudio.tsx
  - After generate job completes, show "Download (Auto stamp)" which POSTs to `/api/ai/compose` with mode=auto and polls status until success.
  - "Open Editor" launches CanvasEditor (apps/web/src/components/AI/CanvasEditor.tsx)
    - CanvasEditor must export to PNG and optionally POST to compose endpoint (mode=editor) to store server copy.

E. Tests for compose:
- tests/ai/compose.test.ts
  - TEST_MODE=true run: enqueue compose job; poll until done; verify resulting file exists and `Content-Type: image/png` on served result.
  - Validate overlay metadata file contains artist/release/watermark fields.

PHASE 4 — CanvasEditor & client UX polish
1. CanvasEditor component:
   - Use createImageBitmap for base image load.
   - Provide controls for artist, release, font family, size, color, watermark toggle, watermark opacity, position.
   - Export via `canvas.toBlob()` and allow direct download plus server POST.
   - Ensure memory cleanup (revokeObjectURL, close ImageBitmap).

2. Default UX:
   - Watermark ON by default for auto-stamp and editor exports.
   - Default position bottom-right, artist top-left or center (design as earlier spec).
   - When user clicks auto-download, they receive server-composed PNG (includes artist + release + watermark).

PHASE 5 — Fallback thumbnail & image handling
1. Ensure public fallback is a PNG (not only SVG):
   - If only fallback-thumbnail.svg exists, produce fallback-thumbnail.png using worker rasterizer and place into apps/web/public/fallback-thumbnail.png.
   - All ImageWithFallback and feed-image helpers must return `/fallback-thumbnail.png` when image missing or HEAD invalid.

2. Ensure no code generates title-on-image thumbnails. Replace that logic if present.

PHASE 6 — Poller admin & settings verification
1. Ensure admin feed-settings page exists and is fully functional:
   - apps/web/app/(admin)/feed-settings/page.tsx
   - apps/web/app/api/admin/feed-settings/route.ts
   - apps/web/app/api/admin/feed-poller/route.ts
2. Verify poller settings persist in dev-data/feed_sources.json and that poller can be started/stopped via API, and that Run Now triggers immediate pass.

PHASE 7 — Diagnostics & performance tests
1. Ensure these scripts exist and are runnable:
   - scripts/diag/check-heavy-css.js
   - scripts/diag/run-scroll-stress.js
   - scripts/diag/check-memory.js
   - scripts/diag/run-lighthouse-local.js (optional if environment permits)

2. Install Playwright if not present and run the scroll stress script for page `/feed`:
   - `pnpm --filter apps/web exec -- playwright test` or run `node scripts/diag/run-scroll-stress.js http://localhost:5000/feed`
   - Report frame drop %, max main-thread task >100ms, and whether results pass thresholds.

3. Run CSS heavy check and report banned properties used in dashboard CSS (backdrop-filter, heavy large-blur, mix-blend-mode, large box-shadow). Dashboard files must not contain these — only landing page exceptions allowed.

PHASE 8 — Unit tests, snapshots, and e2e
1. Unit & snapshot tests:
   - `pnpm --filter apps/web test:unit` and `pnpm --filter apps/web test:snap`
   - Snapshot files must exist for `/dashboard`, `/dashboard/gigs`, `/dashboard/community`.
   - If any snapshot diffs for dashboard routes appear, revert those UI changes (dashboard layout must remain identical) and re-run snapshots.

2. E2E:
   - Playwright tests: `pnpm -w playwright test` (or `pnpm --filter apps/web playwright test`)
   - Must include tests:
     - Landing page load (can be redesign, ok)
     - Dashboard load (snapshot)
     - CoverArtStudio generate->compose->download flow (TEST_MODE)
     - Feed list rendering with fallback thumbnail

PHASE 9 — Memory & concurrency stress (must run)
1. Start worker in TEST_MODE:
   - `TEST_MODE=true node packages/ai/worker.js`
2. Enqueue 3 concurrent compose jobs (server auto-stamp) and run `scripts/diag/check-memory.js`.
3. Heap growth must be under 50MB. If over, reduce concurrency or switch to streaming rasterizer.

PHASE 10 — CI & commit
1. Stage and commit all changes with message:
   - `feat(ai): finish compose+watermark flows, canvas editor, sharp-fallback, tests & diag`
2. Push and produce a final report containing:
   - git commit SHA
   - `pnpm -w tsc --noEmit` output (or list remaining errors)
   - Unit test results summary
   - Snapshot test summary (pass or diff list)
   - Poller simulation log showing disabled sources if any
   - AI worker sample job IDs and final result URLs (for 2 sample jobs)
   - Scroll stress summary (frame drop %, max main-thread block)
   - Memory check summary
   - Confirmation: `/apps/web/public/fallback-thumbnail.png` present and used in sample feed

PHASE 11 — Return structured report to user (machine + human)
Return the following EXACT output in your Replit Assistant reply (append actual outputs):

1) JSON object:
{
  "status": "success" | "partial" | "failed",
  "commit": "<sha>",
  "tsc": "<ok or error summary>",
  "unit_tests": "<summary>",
  "snapshots": "<summary>",
  "poller_simulation": "<excerpt>",
  "ai_worker_jobs": ["<jobId>: <url>", "..."],
  "scroll_stress": "<summary>",
  "css_check": "<summary>",
  "memory_check": "<summary>",
  "notes": ["list any fallbacks used (sharp missing, playwright skipped, etc)"]
}

2) Human summary (5 lines max) with top fixes and next recommended action (if any).

ACCEPTANCE CRITERIA (must be enforced)
- `pnpm -w tsc --noEmit` returns 0 errors OR a documented short list if blocked by environment specifics.
- Unit tests and snapshot tests pass for the web app (dashboard snapshots must not change).
- Compose server-side stamp flow works end-to-end in TEST_MODE and returns PNG.
- Canvas editor can export to PNG and server can store copy.
- Worker handles concurrency safely and memory growth < 50MB during the memory test.
- Fallback thumbnail exists as PNG and is used by feed items without a thumbnail.
- Diagnostics scripts run and produce pass summaries (Playwright optional; if skipped, clearly report how to run locally).
- Commit created and SHA returned.

FINAL NOTES / Helpful tips the assistant may use internally (do not display unless used to fix):
- If sharp fails: use `@svg2png-js` + node streams for rasterization. Keep streaming to files to avoid memory buffering.
- For font inclusion, use Google Inter font subset and place into `apps/web/public/fonts` with `font-display:swap`.
- Use safeFetch everywhere (first read text then JSON.parse) to avoid HTML parse issues.

RUN IT NOW and return the full JSON report + the 5-line human summary.

END OF PROMPT

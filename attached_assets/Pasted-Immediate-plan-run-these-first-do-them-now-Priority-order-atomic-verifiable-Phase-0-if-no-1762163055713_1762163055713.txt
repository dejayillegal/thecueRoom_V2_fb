Immediate plan (run these first — do them now)

Priority order (atomic, verifiable):

Phase 0 (if not already): environment & deps — confirm TEST_MODE and secrets.

Phase 2: AI worker infra (packages/ai) + safeFetch utils + fallback SVG art generator.

Phase 4: Auth & verification routes (/api/auth/signup, /api/auth/check-availability, /api/verification/job/[id]) including password hashing and artist verification job enqueue.

Phase 5 (part): Poller & gigs aggregator packages/feeds/poller.ts + consolidated apps/web/app/api/gigs/india/route.ts (safeFetch + cache + concurrency).

Minimal front-end hooks for Signup+Verification modal wiring and minimal API usage so you can test end-to-end.

Why: Without a stable worker/queue and safe fetch/poller, UIs end up glitching — so we harden the backend first.

Exact commands to run now in Replit shell

Install deps (safe):

pnpm -w install
pnpm --filter apps/web add p-limit cross-fetch nanoid react-virtual
pnpm --filter apps/web add -D vitest playwright
# optional: attempt sharp but handle failure in code
pnpm --filter apps/web add sharp || echo "sharp failed — TEST_MODE fallback will be used"


Create directories (quick):

mkdir -p packages/ai apps/web/app/api/ai apps/web/app/api/auth packages/feeds .local ai-temp


Add the minimal safeFetch & worker scaffolding files (copy/paste from templates I can provide next). For now create placeholder files so TypeScript passes:

# create placeholder files
touch packages/ai/worker.js packages/ai/impl/utils.ts packages/ai/impl/fallback-svg.ts
touch packages/feeds/poller.ts apps/web/app/api/gigs/india/route.ts
touch apps/web/app/api/auth/signup/route.ts apps/web/app/api/verification/job/[jobId]/route.ts


Typecheck:

pnpm -w tsc --noEmit


Run unit-tests (if any exist yet):

pnpm --filter apps/web test:unit || echo "no tests / some tests failing — will add tests next"


Start dev server:

pnpm --filter apps/web dev
# or if repo root has a script:
pnpm dev

Implementation checklist for this initial bundle (Phase 0 → 4)

Back-end must:

 safeFetch helper with AbortController, attempts, exponential backoff and safe JSON parsing fallback.

 packages/ai/worker.js that reads a durable queue and supports job types verifyArtist and fallbackSvgRasterize. Worker writes status JSON files to AI_TEMP_DIR/<jobId>.status.json.

 packages/ai/impl/fallback-svg.ts that exports a generateSVG({preset, seed, artist, release}) returning 1024×1024 SVG string (grain + dark lime/purple gradient).

 apps/web/app/api/auth/signup/route.ts validates input (Zod), stores user in dev JSON DB, hashes password, enqueues verifyArtist for artist signups, returns { userId, verificationJobId? }.

 apps/web/app/api/auth/check-availability/route.ts (email/artistName).

 apps/web/app/api/verification/job/[jobId]/route.ts returns job status from AI_TEMP_DIR.

 packages/feeds/poller.ts with concurrency limit, per-fetch timeout and retry/backoff, source cache, and admin-configurable settings file for poll interval and concurrency.

 apps/web/app/api/gigs/india/route.ts reads from packages/feeds/poller cache and returns normalized events {id,title,venue,city,start,end,url,source}.

Front-end must:

 use-debounce hook.

 Minimal SignupModal wiring: checkbox "Artist sign up" reveals socialProfile input and genre text input (no dropdown).

 After signup for artist, open VerificationModal which polls /api/verification/job/:jobId and shows status.

Diagnostics:

 node scripts/diag/check-heavy-css.js stub that scans CSS for banned properties (we'll run it later).

 node scripts/diag/check-memory.js to run a TEST_MODE worker bloom test.

Acceptance checks (for this bundle):

pnpm -w tsc --noEmit → 0 errors

/api/auth/signup (artist) → returns { userId, verificationJobId }

/api/verification/job/:jobId → returns JSON progress states

packages/feeds/poller can fetch RollingStone and Zomato in TEST_MODE (or use cached fixtures) and GET /api/gigs/india returns normalized events

Worker can process a verifyArtist job in TEST_MODE (reads social URL and sets status file)

What I’ll do next (if you want me to produce code snippets now)

If you want, I can now generate production-ready code for the most critical files in Phase 2–4 (one bundle at a time). I can output paste-ready source for:

packages/ai/impl/utils.ts (safeFetch)

packages/ai/worker.js (TEST_MODE worker + job queue)

apps/web/app/api/auth/signup/route.ts (full Zod schema, password hash)

apps/web/app/api/verification/job/[jobId]/route.ts (reads status)

packages/feeds/poller.ts + a robust apps/web/app/api/gigs/india/route.ts aggregator that uses cached fixtures where remote sources are blocked

These will be implemented with TEST_MODE fallbacks (no HF or Playwright required), streaming file writes, and include JSDoc comments.

Quick risk / blocking checklist

BookMyShow / DICE / Swiggy / Paytm Insider often block automated requests (403). We'll:

Prefer official APIs or RSS feeds.

Use cached fixtures and admin-configurable sources.

Add Playwright headless scraping as optional (requires Playwright binaries and permissions; may not run in Replit). We'll detect its availability and fallback gracefully.

sharp native module may fail to install on Replit; code must gracefully fallback to pure-SVG rasterization.

Playwright and electron imports may not run in Replit; diagnostic scripts will mark them skipped.

Execution: next CLI to paste (one-shot)

If you want me to produce the code now, paste this into Replit console to create the first real files and run checks (it creates placeholders and runs typecheck):

# create folders + placeholder files (so tsc passes)
mkdir -p packages/ai/impl apps/web/app/api/auth apps/web/app/api/verification packages/feeds .local
cat > packages/ai/impl/utils.ts <<'TS'
/**
 * safeFetch utility: Abort + retry + backoff + safe json
 */
import fetch from 'cross-fetch';
export async function safeFetch(url:string, opts:any={}) {
  const timeout = opts.timeout ?? 15000;
  const attempts = opts.attempts ?? 3;
  const headers = opts.headers ?? {};
  const signal = opts.signal;
  let lastErr = null;
  for (let i=0;i<attempts;i++){
    const controller = new AbortController();
    const timer = setTimeout(()=>controller.abort(), timeout);
    try {
      const res = await fetch(url,{headers,signal: signal ?? controller.signal});
      clearTimeout(timer);
      const contentType = res.headers.get('content-type') ?? '';
      if (contentType.includes('application/json')) {
        const json = await res.json();
        return { ok:true, status: res.status, json };
      } else {
        const text = await res.text();
        return { ok:false, status: res.status, text };
      }
    } catch (err) {
      lastErr = err;
      const backoff = 500 * Math.pow(2,i);
      await new Promise(r=>setTimeout(r, backoff));
    }
  }
  throw lastErr;
}
TS

# placeholders
for f in packages/ai/worker.js apps/web/app/api/auth/signup/route.ts apps/web/app/api/verification/job/[jobId]/route.ts packages/feeds/poller.ts apps/web/app/api/gigs/india/route.ts; do
  mkdir -p $(dirname $f)
  echo "// placeholder for $f" > $f
done

pnpm -w tsc --noEmit || true


That bootstraps the repo so typecheck runs and we can iterate.

Summary (short & sweet)

Recommended immediate focus: Phases 0→4 (env, worker, auth, poller).

I gave you exact shell commands, acceptance checks, and the minimal file scaffolding to run now.

Next, I can paste full production-ready source code for the critical files (safeFetch, worker, signup route, poller, gigs aggregator) so you can drop them into Replit and run the acceptance checks.
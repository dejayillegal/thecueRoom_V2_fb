TASK: Full app performance & memory audit + automated fixes — make thecueRoom fast, smooth, and leak-free. Run tests and report. Make changes, run diagnostics, commit.

GOALS (short):
1. Zero UI/scroll lag (virtual lists, lazy images, IntersectionObserver).
2. Fix memory leaks and event listener/interval leaks.
3. Fast page loads: SSR-friendly, lazy load heavy components, code-split.
4. Background heavy work: web workers/child_process for AI, image rasterize.
5. Robust network layer: AbortController, timeouts, safeParseJSON.
6. Add automated perf & memory tests: Lighthouse baseline, heap snapshots, stress test.
7. Run all unit/e2e tests, fix failing files, produce verification report.

ACTIONS — implement, test, and commit (do not ask questions):

A. STATIC PRECHECKS (run first)
- Run: pnpm -w tsc --noEmit
- Run: pnpm install (if needed)
- Run: pnpm --filter apps/web test:unit
- Collect initial failures and include them in the report.

B. QUICK FIXES (apply immediately)
Edit or create these files (search & update equivalents if path differs):

1. apps/web/src/lib/fetcher.ts
- Implement `safeFetch(url, opts)` wrapping fetch with AbortController, default 15s timeout, and `safeParseJSON(text)` that returns `{ ok: false, text }` when JSON parsing fails. Use for all network calls.

2. apps/web/src/hooks/use-debounce.ts
- Ensure hook cleans up timers on unmount and uses `useRef` for timer id.

3. apps/web/src/hooks/use-safe-interval.ts
- Implement and use everywhere that previously used setInterval directly. Clean up on unmount.

4. apps/web/src/hooks/use-event-listener.ts
- Add passive option for wheel/touch listeners; auto cleanup in useEffect return.

5. apps/web/src/components/VirtualList.tsx (or integrate react-virtual)
- Provide virtualization for long lists (forum threads, news feed, gig lists). Use virtualization library (react-virtual) or implement windowing. Ensure item heights or estimate heights.

6. apps/web/src/components/ImageWithFallback.tsx
- Lazy-load images via `loading="lazy"` + `IntersectionObserver` fallback. Add onError to swap to placeholder. Use progressive low-res placeholder (LQIP) if available.

7. apps/web/src/components/Sidebar.tsx / Header.tsx
- Memoize components with React.memo where appropriate.
- Avoid inline function props where possible; use useCallback.
- Ensure routes and navigation use `next/navigation` correctly — avoid heavy calculations in render.

8. apps/web/src/hooks/use-shared-socket.ts
- Implement single WebSocket connection (singleton pattern). Ensure close on page unload.

9. apps/web/app/globals.css
- Add `touch-action: pan-y` and `-webkit-overflow-scrolling: touch` where mobile scrolls are used. Remove expensive CSS like `filter: blur()` on large elements, move to smaller overlays.

C. SCROLL & UI JANK (critical)
- Replace any long blocking synchronous operations in render with:
  - requestIdleCallback (or setTimeout 0) for non-critical.
  - Web Worker for heavy CPU tasks (image composition, complex SVG rasterization).
- Make long lists virtualized.
- For infinite scroll / feed, use IntersectionObserver to load next page; mark network requests cancellable with AbortController so quick scrolling cancels prior fetches.
- Use CSS `will-change: transform` sparingly only on animated elements.
- Use hardware-accelerated transforms (translate3d) for sticky/fixed animations.

D. MEMORY LEAK HUNT
- Add scripts/diag/check-memory.js that:
  - Starts the app in TEST_MODE.
  - Runs 3 concurrent flows (open feed, generate fallback covers, simulate socket events).
  - Takes Node heap snapshots before/after (v8.getHeapSnapshot or `--inspect` + heap dump).
  - Fails if heapUsed grows by >50MB across tests.
- Add diagnostics to server: monitor active listeners count, active timers; log abnormal growth.

E. BACKGROUND WORK & AI
- Move heavy AI-rendering / sharp rasterize to child_process or Node worker threads (packages/ai/worker.js).
- Jobs: enqueue, do not block request thread, stream progress via SSE or polling.
- Limit concurrency (AI_WORKER_CONCURRENCY env var).
- Use streaming writes to disk to avoid buffering big files into memory.

F. ROBUSTNESS & SAFE JSON
- Replace all `res.json()` calls with:
  const text = await res.text();
  try { data = JSON.parse(text); } catch(e) { /* log serverHTML */ return structured error JSON; }
- Ensure any HTML error page is parsed safely and returned as string inside an error object (no crash).

G. TESTING — automated (do this after fixes)
1. Unit tests:
   - pnpm --filter apps/web test:unit
   - Fix failing tests

2. E2E tests:
   - Use Playwright or Puppeteer e2e suite: e2e/dashboard-load.spec.ts and e2e/forum-scroll.spec.ts
   - Run: pnpm --filter apps/web test:e2e (or pnpm -w test:e2e if configured)
   - Add a test to confirm no console errors during page load.

3. Performance tests:
   - scripts/diag/run-lighthouse-local.js — run Lighthouse headless against http://127.0.0.1:3000 (or replit url).
   - Accept baseline: Lighthouse score >= 90 PWA/Performance (aim for 95). If below, produce top 5 actionable items.

4. Memory tests:
   - node scripts/diag/check-memory.js — should pass memory growth thresholds.

H. STRESS / STABILITY
- scripts/diag/stress-feed.js — spawn 200 concurrent feed fetches with small delays and confirm no OOM, no event-loop blocking over 500ms.
- Confirm the server returns 200 under stress and that the frontend remains interactive.

I. LINT/TSC
- pnpm -w tsc --noEmit
- pnpm -w eslint --fix
- Fix all type errors. If tests fail due to types, patch.

J. MONITORING & METRICS (add lightweight)
- Add perf marks via `performance.mark()` around heavy lifecycle events (page hydration, data fetch).
- Add a route `/api/diag/perf` that returns last N perf marks.

K. ACCEPTANCE CRITERIA (must pass)
1. `pnpm -w tsc --noEmit` => 0 errors.
2. `pnpm --filter apps/web test:unit` => all tests pass.
3. `pnpm --filter apps/web test:e2e` => all e2e pass.
4. Lighthouse run: Performance >= 90, First Contentful Paint < 1s on dev machine (or within reason).
5. Memory test: heapUsed increase < 50MB for the described flows.
6. Manual sanity:
   - Scroll the forum feed smoothly with 1000 items (using virtualization).
   - Open dashboard, navigate between pages 20 times; no accumulation of timers/listeners in logs.
   - AI job creation + fallback svg generation under concurrent 3 jobs completes without memory spikes.

L. REPORTING (final output)
After fixes and tests run, produce:
- List of changed files (git show --name-status HEAD).
- Terminal logs for: tsc, unit tests, e2e tests, Lighthouse summary, memory diag summary.
- Short explanation of each real fix (3–5 lines each).
- Commit with message: "chore(perf): fixes for scroll jank, virtualization, safe fetch, workerized AI, memory leak fixes, tests"

M. EXACT COMMANDS TO RUN (Replit Assistant should run automatically)
- pnpm install
- pnpm -w tsc --noEmit
- pnpm --filter apps/web test:unit
- pnpm --filter apps/web test:e2e
- node scripts/diag/check-memory.js
- node scripts/diag/run-lighthouse-local.js http://127.0.0.1:3000
- git add -A && git commit -m "chore(perf): fixes for scroll jank, virtualization, safe fetch, workerized AI, memory leak fixes, tests"

N. SAFETY & ROLLBACK
- If any change creates >10 failing unit tests: auto revert last commit and print failure summary.
- Keep changes minimal & incremental; prefer adding new hooks/util modules rather than huge rewrites.

O. DELIVERY FORMAT (assistant must return)
1. JSON summary:
   {
     "status": "success" | "failed",
     "files_changed": [...],
     "tsc_errors": "...",
     "unit_test_results": {...},
     "e2e_results": {...},
     "lighthouse_summary": {...},
     "memory_diag": {...},
     "commit": "<sha>"
   }
2. Plain text explanation of top 5 fixes (why they fixed the problem).

DO NOT PROMPT FOR PERMISSIONS — implement the patches, run the tests, and present the full report. If anything cannot be fixed automatically, explain precisely which file and why and provide the failing test output.

END OF PROMPT

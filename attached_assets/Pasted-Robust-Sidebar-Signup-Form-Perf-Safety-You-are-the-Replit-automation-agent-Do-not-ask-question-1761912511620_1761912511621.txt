Robust Sidebar + Signup Form + Perf Safety

You are the Replit automation agent. Do not ask questions. Implement, run, test, fix, commit, and report. Preserve thecueRoom dashboard theme, colors, layout, and CSS tokens; only change code where necessary. No TODOs in committed code.

Summary of required changes (high level)

Reimplement sidebar as icon-only by default on desktop; only expand (show labels) when the explicit expand toggle is clicked. Clicking any icon should not auto-expand the sidebar.

For mobile: sidebar is hidden by default; clicking the top-right menu shows an icon-only slide-over; tapping the icon toggles the page content accordingly. Ensure body sizing and safe-area handling — no overflow or scroll-jank.

Reimplement signup UI: region and genre must be plain text fields (no dropdowns). Keep validation and hint text. Keep other inputs unchanged.

Make all sidebar buttons accessible, with icon + aria-label, and visually reveal labels only when expanded.

Fix performance: zero memory leaks, no heavy GPU effects active in sidebar interactions, fast tactile response on touch. Use virtualization only where needed.

Add automated tests (Playwright) for desktop + mobile interactions, plus memory and scroll stress diagnostics.

Add small diagnostics that detect event listener leaks and long-running timers.

Commit with a single message and produce an automated verification report.

Files to create/update (exact or equivalent)

Make these changes and tests. If files already exist, update them cleanly.

Frontend

apps/web/src/components/Sidebar/Sidebar.tsx

Replace current sidebar implementation with the new component described below.

apps/web/src/components/Sidebar/SidebarItem.tsx

New small component: icon + optional label (visible only when expanded). Accepts icon, label, href, onClick.

apps/web/src/hooks/usePersistentToggle.ts

New hook: boolean toggle persisted to localStorage (key: tcr:sidebar-expanded) with safe feature detection and server-side rendering guard.

apps/web/src/hooks/use-fast-touch.ts

New hook: fast tap handling for touch (uses passive listeners, prevents double-tap delays).

apps/web/src/components/ui/IconLabelButton.tsx

Ensure this exists (or update) to always render icon + visible label when instructed. Use keyboard focus ring, accessible role and aria attributes.

apps/web/src/pages/_app.tsx (or layout)

Ensure body and html CSS classes include touch-action: manipulation and -webkit-tap-highlight-color: transparent for better touch response. Ensure safe-area paddings for mobile.

apps/web/app/(auth)/signup/page.tsx (or apps/web/src/pages/signup.tsx)

Update signup form: region and genre → regular text <input> fields (no select/combobox). Keep validation messages and remap any server API fields accordingly.

apps/web/src/lib/ui-safety.ts

Small utilities: isClient(), safeLocalStorageGet, safeLocalStorageSet for SSR-safe operations.

CSS

apps/web/app/globals.css

Add .sidebar-icon-only / .sidebar-expanded rules; ensure animations are prefers-reduced-motion safe and use transform/opacity only (no expensive filters). Add .no-heavy-gpu class applied globally to prevent heavy effects on dashboards.

apps/web/src/styles/sidebar.css

Scoped styling for the new sidebar; compact and optimized.

Diagnostics & Tests

apps/web/tests/e2e/sidebar.spec.ts (Playwright)

Desktop: assert sidebar icon-only by default, clicking icons doesn’t expand, clicking expand toggle expands and shows labels, persistent across reloads, keyboard accessible.

Mobile: emulate mobile viewport, assert sidebar hidden, tapping menu shows icon-only slide-over, tapping expand inside slide-over toggles expanded labels.

scripts/diag/check-listeners.js

Node script to analyze running page (via Playwright) for event listeners count on window and key elements. Fail if listener count increases on repeated navigation indicating leak.

scripts/diag/check-timers.js

Detect un-cleared intervals/timeouts by polling during automated scenario; fail if long-lived timers grow unexpectedly.

scripts/diag/run-scroll-stress.js (update to test sidebar interactions)

Stress test toggling sidebar open/close 500 times and ensure no long main-thread blocks.

scripts/diag/check-memory.js (update)

Run UI navigation & sidebar toggles loop, and record server + client heap. Fail if heap increases > 50MB server or > 30MB client.

Utilities

apps/web/src/lib/safe-fetch.ts (if not already)

Ensure safe JSON parsing (no response.json() on HTML).

Component behavior — explicit spec (Sidebar.tsx)

Desktop default state: icon-only vertical bar (left).

Width collapsed: 64px (icon centered).

Expand toggle control at the bottom: a distinct Expand button (icon + hidden label). Only this toggle expands/collapses the sidebar.

Clicking any nav icon will navigate/trigger but must not expand the sidebar.

Keyboard: Tab focuses icons, Enter/Space triggers; Expand toggle is keyboard accessible.

Expanded state: width 240px, shows labels, sub-sections, nested menu if any.

Expanded state persists in localStorage via usePersistentToggle.

Mobile behavior:

By default, only top menu button (hamburger) visible in header. When tapped, a slide-over panel from left appears showing icon-only vertical list (like desktop collapsed). Inside this panel there is the same Expand toggle to switch to expanded view (labels visible) while panel is open. Tapping outside or pressing Escape closes panel.

Body behind the slide-over must be inert (use inert attribute or aria-hidden) and not scroll while panel open.

Slide-over animations must be hardware-accelerated (transform: translateX) and have will-change: transform, but suppressed when prefers-reduced-motion is set.

Touch responsiveness:

Attach touch handlers using passive: true where appropriate; use use-fast-touch hook to eliminate 300ms delays on older browsers.

All interactive targets must have a minimum hit-area of 44×44 px at mobile breakpoint (validate via test).

Performance & memory:

No DOM reflows on hover/scroll. Avoid heavy CSS filters and box-shadows—use minimal shadows with box-shadow: 0 2px 6px rgba(...) only.

Event listeners added on mount must be removed on unmount. Use React useEffect cleanup patterns.

No global setInterval without cleanup; use use-safe-interval hook with cleanup on unmount.

API / Signup changes

Signup form change:

region & genre replaced with regionText, genreText <input type="text"> fields.

Keep server payload names compatible — map to expected backend fields (region and primaryGenre) when posting.

Add front-end validation: non-empty, max lengths (region ≤ 60, genre ≤ 60), sanitize inputs.

Add unit test apps/web/tests/unit/signup.spec.ts to validate form behavior.

Tests & acceptance criteria

Agent must run these checks and ensure all pass. If something fails, fix it and re-run.

Install & run commands (agent should run)
pnpm -w install
pnpm --filter apps/web install
pnpm --filter apps/web test:e2e  # Playwright tests
node scripts/diag/check-listeners.js
node scripts/diag/check-timers.js
node scripts/diag/run-scroll-stress.js
node scripts/diag/check-memory.js
pnpm -w tsc --noEmit

Acceptance criteria (must be enforced)

Desktop default: sidebar is collapsed (icon-only). Clicking any icon does not expand.

Expand toggle: expands/collapses with persisted state across reloads.

Mobile: slide-over menu shows icon-only; Expand toggle inside slide-over reveals labels; body scrolling disabled while open.

Signup: region & genre are text inputs, validated, saved correctly to server payload.

Accessibility:

All sidebar buttons have aria-label.

Keyboard navigation works; Expand toggle focused via Tab.

Tap targets ≥ 44×44 px at mobile breakpoint.

Performance:

No long tasks > 120ms triggered by sidebar open/close on both desktop and mobile per scroll-stress test.

No event-listener leaks after repeated open/close (check-listeners must show stable counts).

No unbounded timers (check-timers).

Memory:

check-memory.js must report client + server heap increases within thresholds (server < 50MB, client < 30MB after stabilization).

Typecheck:

pnpm -w tsc --noEmit must succeed (0 errors).

Tests:

Playwright sidebar.spec.ts must pass all cases.

Coding rules & style

Use React + TypeScript (existing project stack). Keep types strict and add Zod validation for any client->API payloads as needed.

Avoid side-effects in render. Use useEffect for DOM ops and cleanup.

Use CSS variables defined in the project for colors and spacing. Do not change root tokens.

Keep animations prefers-reduced-motion aware.

All new code must include unit tests and JSDoc comments.

Diagnostics & reporting

Agent must produce a reports/sidebar-fix.json containing:

{
  "status":"success"|"failed",
  "commit":"<sha>",
  "tsc":"ok or errors",
  "playwright": { "passed": n, "failed": m, "report": "apps/web/tests/playwright-report.html" },
  "listenersCheck": { "initial": x, "afterLoops": y },
  "timersCheck": { "foundLongLived": false },
  "scrollStress": { "frameDropsPct": 0.5, "maxLongTaskMs": 42 },
  "memory": { "serverHeapDeltaMB": 12, "clientHeapDeltaMB": 8 },
  "notes":[ "...any fallback used..." ]
}


Also produce a 5-line human summary of changes and any recommended next steps (optional features).

Commit message

When done, commit everything with a single commit:

feat(ui): robust icon-only sidebar (desktop+mobile), persistent expand toggle, signup text fields, tests & diagnostics

If anything cannot be completed in Replit due to env limits

Implement TEST_MODE fallbacks for event diagnostics and memory checks.

Document exact local commands the developer must run to finish (e.g., pnpm --filter apps/web add playwright or RUN_IN_LOCAL=true pnpm test:e2e).

The agent must still create code and tests, mark them PASS/FAIL accordingly, and report which steps were skipped due to environment.
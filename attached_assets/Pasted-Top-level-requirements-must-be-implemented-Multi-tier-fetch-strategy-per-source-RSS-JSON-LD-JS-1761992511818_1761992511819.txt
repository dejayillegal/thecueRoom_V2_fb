Top-level requirements (must be implemented)

Multi-tier fetch strategy per source: RSS/JSON → LD+JSON (structured) → sitemap/ics → hidden XHR endpoints → headless rendered page. Stop after first success.

All heavy/JS-rendered scraping happens in a server-only headless worker (child process or worker_threads) — never bundle Playwright into client code.

For sites that block cloud IPs (BookMyShow often does), add optional SCRAPING_PROXY_URL support (admin-config), and a recommendation to run poller from a VM or small VPS IP if proxies are not permitted.

Robust caching: immediate return of cached events, background refresh. Use stale-while-revalidate pattern: respond fast, update in background.

Fix AbortController usage everywhere (new controller per request; clear timers properly) to eliminate the Controller is already closed uncaughtException.

Implement header and request variance strategies (multiple user agents, referer, accept-language) and cookie handling for endpoints that require it.

Add admin UI toggles to force headless for a source, enable proxy for a source, or temporarily disable a source.

Provide full telemetry/logging per source (attempts, method used, status, error) and a diagnostic endpoint /api/admin/feeds/diagnostics.

Acceptance checks and tests must run and pass locally in Replit dev environment (subject to environment constraints).

Files to add / update (exact names; assistant should create/modify these)

A. Canonical helpers (server-side)

apps/web/src/lib/safe-fetch.ts

Ensure single exported safeFetch(url, opts) implementation. Enforce: create new AbortController() every attempt; setup and clear timeouts with const id = setTimeout(() => controller.abort(), timeout) and clearTimeout(id) in finally. Return structured { ok, status, responseText?, json?, headers?, error }.

packages/feeds/cache.ts

LRU TTL cache read/write used by poller and API route.

B. Headless worker (server-only, do not import in client bundle)

packages/feeds/headless-worker.js

Worker process using Playwright (chromium) or Puppeteer (choose Playwright by default if Playwright available); accepts IPC jobs: { jobId, url, waitForSelector?, emulateMobile?:bool, preferHeaders?:{}, screenshot?:false }. Returns { jobId, ok, html, statusCode, error }.

Respect env vars: PLAYWRIGHT_ENABLED (false by default), PLAYWRIGHT_CONCURRENCY, PLAYWRIGHT_HEADLESS_ARGS.

Implement robust try/catch and gracefully exit on fatal errors. Use process.on('uncaughtException') to log and avoid crashing main process.

packages/feeds/headless.ts

Node-side manager that fork()s worker and exposes renderPage(url, opts) returning parsed HTML. Concurrency controlled with p-limit.

C. Per-source adapters (server-side)
Create or update adapters to follow this algorithm in sequence (stop on first success):

Try official RSS / public JSON / API endpoints with safeFetch.

If not found, fetch landing or listing pages and parse <script type="application/ld+json"> for @type: Event using Cheerio.

If none, fetch sitemap.xml, .ics, or feed files and parse.

Try hidden XHR endpoints — inspect site network patterns and call same endpoints (e.g., BookMyShow internal APIs). Use header variants.

If still fails and PLAYWRIGHT_ENABLED=true, call renderPage(url, { waitForSelector }) via headless and parse post-rendered HTML.

If still fails, return empty array but log structured failure.

Adapters to add/update:

packages/feeds/sources/bookmyshowIndia.ts

Try known endpoints: BookMyShow often structures events per city — attempt forms like https://in.bookmyshow.com/ city pages or https://in.bookmyshow.com/servlets/movies (inspect). If HTML blocked, use headless.

When 403 encountered, mark as blocked = true in diagnostics and attempt headless with normal UA and then with mobile UA.

packages/feeds/sources/zomatoLive.ts (Zomato Live)

Confirm earlier success path (your logs show Zomato OK). Keep it as primary.

packages/feeds/sources/steppinoutSwiggy.ts

Try https://steppinout.swiggy.com/api/events or https://steppinout.swiggy.com/events patterns; otherwise use headless.

packages/feeds/sources/paytmInsider.ts

packages/feeds/sources/sortmyscene.ts

packages/feeds/sources/skillbox.ts

packages/feeds/sources/diceIndia.ts

packages/feeds/sources/rollingstoneIndia.ts (keep as fallback source — already working)

Implementation details for each adapter:

Wrap all fetches in safeFetch.

For HTML parsing use cheerio.load(html); parse LD+JSON first.

Normalize to NormalizedEvent:

type NormalizedEvent = {
  id: string;
  title: string;
  start: string; // ISO
  end?: string;
  venue?: string;
  city?: string;
  url?: string;
  ticketUrl?: string;
  source: string;
  raw?: any;
}


Cache HEAD checks for images or ticket links (short TTL).

D. Aggregator & Poller (workerized)

packages/feeds/poller.ts (or update existing)

For each source:

Try adapter.fetch().

If adapter returns 0 events and error shows 403 or network-block, attempt header/cookie variations and then headless (if allowed).

Use p-limit(POLL_CONCURRENCY) for concurrent source fetches.

Save results to packages/feeds/cache.ts.

Log per-source diagnostics to logs/feed-<source>.log including method used: rss|json|ld|xhr|sitemap|headless.

If headless is used more than HEADLESS_THRESHOLD times, set a cool-off or admin alert.

E. API route change

apps/web/app/api/gigs/india/route.ts

Immediately return cached events (if fresh) with { fromCache:true }.

If cache stale, trigger poller.runBackground() and still return cached events or an empty response with revalidateIn info so UI shows “Checking for updates…”.

Support ?force=true to force synchronous refresh (subject to timeout cap of 7s) — useful for admin run-now.

Ensure valid JSON output and proper Cache-Control.

F. Admin & Diagnostics

apps/web/app/(admin)/feed-settings/page.tsx

Add toggles: forceHeadless, useProxy, enabled, pollInterval.

Show last fetch summary for each source (lastFetchedAt, status, methodUsed, itemsCount).

/api/admin/feeds/diagnostics returns last N logs for each source.

G. Proxy support

Add env var .env: SCRAPING_PROXY_URL (optional). If set, safeFetch should route requests through the proxy for specific sources flagged in admin settings.

Provide secure note: using third-party proxies for scraping may violate TOS; prefer self-hosted small VPS for polling if possible.

H. Fix AbortController bug

Audit all usages: safeFetch, poller, headless manager. Ensure:

New AbortController() per attempt.

clearTimeout(timeoutId) before exiting the fetch function (finally block).

Do not reuse controller across attempts.

Wrap response body handling and res.text() / res.json() safely (use await res.text() then attempt JSON.parse in try/catch). Return structured error if HTML returned.

I. Logging & failure heuristics

If a source returns HTTP 403 more than N consecutive polls, mark blocked and reduce poll frequency; write alert into admin logs.

If ENOTFOUND, set disabled for a cool-off and retry at 3x interval.

J. Tests & Diagnostics (run them)

scripts/diag/gigs-poller-test.js:

Simulate poll once for each source, run with FORCE_HEADLESS=true and PLAYWRIGHT_ENABLED=true (if available) to validate headless success.

Acceptance checks to run on Replit:

pnpm -w tsc --noEmit => 0 errors

node packages/feeds/poller.js --once --debug => should output per-source method used and event counts (if the environment blocks headless, still produce logs of failure methods)

curl "http://localhost:3000/api/gigs/india" | jq => JSON with events.count > 0 and meta.sources[] info

Run the diagnostic script: node scripts/diag/gigs-poller-test.js and paste output

K. UI behavior

When API returns fromCache:true and meta.isRefreshing:true, show a small banner “Updating events — serving cached results” instead of “No events found”.

Expose “Run Now” button in admin feed settings to manually trigger poller for a single source.

L. Failure modes & runbook (assistant must include in final reply)

If BookMyShow continues to return 403 from Replit IPs:

Recommendation A: set SCRAPING_PROXY_URL to a proxy you control (self-hosted server or paid proxy) and enable proxy for BookMyShow only.

Recommendation B: run poller from a small VPS in your region (cheapest $5/month droplet) and point the app to that poller via webhook/cached export.

Document exact safeFetch proxy env usage and admin toggle steps.

M. Deliverables (assistant must produce after implementation)

Patch applied to repo for files changed (list with git show --name-status HEAD).

Run outputs for acceptance checks listed above (TS errors, poller output, curl JSON).

Example diagnostics log excerpts showing how BookMyShow was fetched (method used: headless or xhr) and whether 403 persisted.

If proxies required, explicit instructions to set SCRAPING_PROXY_URL or to run poller externally with exact commands.

Environment variables to add/update in .env.example

PLAYWRIGHT_ENABLED=false
PLAYWRIGHT_CONCURRENCY=1
HEADLESS_THRESHOLD=10
POLL_CONCURRENCY=4
POLL_INTERVAL_SECONDS=300
SCRAPING_PROXY_URL=       # optional proxy (http://user:pass@host:port)
FEEDS_CACHE_TTL_SECONDS=900


Security & ethics note (assistant must add)

Scraping some sites may breach their Terms of Service. Implement polite scraping: identify the app with a User-Agent, keep low request rates, honor robots.txt where reasonable, and prefer official APIs or partnerships for production.

Quick troubleshooting tips to paste into the repo README (assistant should add)

If BookMyShow returns 403 on Replit but works locally: run poller from a home/dev machine or small VPS. Example command for remote poller:

# On your VPS
git clone ... && pnpm install
PLAYWRIGHT_ENABLED=true PLAYWRIGHT_CONCURRENCY=1 node packages/feeds/poller.js --daemon


To enable proxy for a single source, toggle admin setting useProxy=true for that source and set SCRAPING_PROXY_URL in .env.

Run this now, make the code edits, run the tests and poller once, and produce a short report with:

pnpm -w tsc --noEmit output

node packages/feeds/poller.js --once --debug output

curl "http://localhost:3000/api/gigs/india" | jq output

logs/feed-bookmyshow.log excerpt (last 30 lines)

If BookMyShow remains 403, include the recommended next step (proxy or VPS) and sample proxy format to use.